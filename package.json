{
  "name": "Jinghong-UI",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "turbo run build",
    "build:components": "pnpm -F components run build && node scripts/build_publish.js",
    "dev": "turbo run dev",
    "new": "node scripts/new_components.js",
    "publish": "node scripts/publish.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "turbo": "^1.10.12"
  },
  "dependencies": {
    "fs-extra": "^11.1.1"
  }
}
